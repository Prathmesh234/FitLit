# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# uv / virtualenv
.venv/
venv/

# Environment & secrets (OAuth tokens, client secrets)
.env
.env.*
# the .env.example template is committed; it holds no secrets
!.env.example
*.secret
tokens.json
credentials.json

# FitLit runtime output (SQLite databases + scheduler/rate-limit state)
data/db/
data/state/

# Editor / OS
.idea/
.vscode/
.DS_Store
