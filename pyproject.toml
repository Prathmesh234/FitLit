[project]
name = "fitlit"
version = "0.1.0"
description = "Cron-job based fetcher for Fitbit / Google Health API data"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pyyaml>=6.0.3",
]
